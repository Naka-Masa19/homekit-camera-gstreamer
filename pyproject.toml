[project]
name = "homekit-camera-gstreamer"
version = "1.0"
requires-python = ">=3.10"
dependencies = ["HAP-Python"]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"